<!DOCTYPE html>
<html lang="en">
  <head>
    <% include ./partials/_head %>
  </head>
  <body style="padding-left: 15px">
    <header>
        <% include ./partials/_header %>
    </header>
    <table class="table">
      <tr>
        <th> ShortURL </th>
        <th> Visits </th>
        <th> Unique visitors </th>
      </tr>
      <tr>
        <td><%= shortURL %></td>
        <td><%= visits %></td>
        <td><%= uniqueVisits %></td>
      </tr>
    </table>
    <table>
      <form action="/urls/<%= shortURL%>?_method=PUT" method="POST">
        <input id="URL" type="text" name="update" value="<%= fullURL%>" style="width: 300px">
        <input type="submit" value="Update" style="margin-left
        : 10px">
      </form>
      <form method="POST" action="/urls/<%= shortURL%>/delete?_method=DELETE">
        <input type="submit" value="Delete" style="margin-left
        : 10px">
      </form>
    </table>
  </body>
</html>
